spring:
  main:
    lazy-initialization: true
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        format_sql: false

logging:
  charset:
    console: UTF-8
